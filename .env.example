# Clerk publishable key (safe to expose in the browser bundle).
# Local: copy to .env.local
# GitHub Pages: set Actions secret VITE_CLERK_PUBLISHABLE_KEY and pass it into the build step.
VITE_CLERK_PUBLISHABLE_KEY=
